import React, { Component } from 'react';
import { Container, Text } from './styles';

export default class Login extends Component {
    render() {
        return (
            <Container>
                <Text> Login, Hello World </Text>
            </Container>
        )
    }
}
